fn monkey() -> int {
  var x = 12345;
  ret x;
}

// this is main function
// the entry point
fn main() -> int {
  var b = 1;

  // still has problem in if-else
  if (b) {
    b = 2;
  } else {
    b = 3;
  }

  print(b);

  ret 0;
}