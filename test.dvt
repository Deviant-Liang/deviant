fn get42() -> int {
  print(42);
  ret 42;
}

// this is main function
// the entry point
fn main() -> int {
  var x = 1;
  var y = 32;
  x = get42();
  // this is a comment
  // this is another comment
  y = x;
  print(x);
  ret y;
}